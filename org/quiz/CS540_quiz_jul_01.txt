Note to students: This quiz is not a memory test. Open a Colab notebook if you are unsure — that is exactly the engagement we are looking for.

The quiz covers Week 6 content: DataCamp Supervised Learning with scikit-learn, Chapter 4 — preprocessing categorical features, handling missing data, feature scaling, pipelines, and evaluating multiple models. Additional questions cover classification metrics from earlier in the course (confusion matrix, precision, recall, F1). Auto-generated by Claude from transcripts; checked by the instructor. Flag mistakes for bonus points!

Each question is worth 1 point. Total: 25 points.

1. Why does scikit-learn require all features to be numeric with no missing values before training?

a) Python dictionaries cannot hold mixed data types
*b) With real-world data, features are rarely numeric with no missing values — preprocessing is needed before models can be built
c) sklearn stores data internally as CSV files, which only support strings
d) Neural network backends require float32 input in all cases

2. What is a dummy variable in the context of encoding categorical features?

a) A placeholder variable used during debugging
*b) A binary feature that equals 1 if an observation belongs to that category and 0 otherwise
c) A variable that stores the category name as a string label
d) A variable created to replace missing values

3. Why is drop_first=True recommended when calling pd.get_dummies?

a) It removes the majority class to balance the dataset
b) The first column always contains the target variable
*c) Dropping one dummy removes redundant information — if all other dummies are 0, the dropped category is implied
d) It prevents the original categorical column from being included twice

4. In the music dataset example, how many binary dummy columns are created for a genre feature with 10 values, and why?

a) 10, because each genre needs its own column
*b) 9, because one genre is implied when all others are 0 — keeping all 10 would duplicate information
c) 1, because genres are ranked and stored as integers
d) 10, then one is removed only when using logistic regression

5. When calling df.dropna with a subset argument, what does subset specify?

a) The fraction of rows to drop
*b) The columns to check — rows with missing values in those columns are removed
c) The rows to inspect for missing values
d) A threshold: drop columns with more than that many missing values

6. What is data leakage in the context of imputation?

*a) Computing imputation statistics on the full dataset before splitting — test set information contaminates the training process
b) Training data being exposed to end users at inference time
c) Using the wrong strategy (mean vs. median) for a given feature type
d) Applying a fitted imputer to new data it has not seen before

7. What strategy does SimpleImputer use by default, and when should you switch to most_frequent instead?

a) Default is median; switch to most_frequent for numeric features with outliers
b) Default is most_frequent; switch to mean for numeric features
*c) Default is mean; switch to most_frequent for categorical features where a mean is meaningless
d) Default is zero; switch to most_frequent when classes are balanced

8. Why call fit_transform on training data but only transform on test data?

a) fit_transform returns a DataFrame while transform returns an array
*b) Fitting learns statistics from training data only — using those same statistics on the test set prevents leakage
c) transform raises an error on arrays the imputer has not seen before
d) fit_transform is faster and can process larger arrays in one pass

9. What is a scikit-learn Pipeline?

a) A generator that yields mini-batches of training data
*b) An object used to run a series of transformations and build a model in a single workflow
c) A scheduler that parallelizes cross-validation across CPU cores
d) A utility for loading data from multiple file formats at once

10. Why does the performance of KNN depend heavily on whether features are scaled?

a) KNN uses matrix inversion, which is numerically unstable on unscaled data
b) KNN is a linear model and linear models require standardized inputs
*c) KNN classifies by distance — features on larger scales dominate the distance calculation and drown out smaller-scaled features
d) KNN uses gradient descent, which converges faster on scaled features

11. What does StandardScaler do to each feature column?

*a) Subtracts the mean and divides by the standard deviation, centering the feature at 0 with variance 1
b) Rescales values to the range [0, 1] using the column minimum and maximum
c) Clips values more than 3 standard deviations from the mean
d) Applies a log transformation to reduce right skew

12. In the chapter example, KNN accuracy on unscaled data was 0.53. After adding StandardScaler inside a pipeline, accuracy rose to 0.81. What does this show?

a) Scaling always improves accuracy by at least 50 percent
b) StandardScaler is more powerful than MinMaxScaler for KNN
*c) Scaling can dramatically change KNN results — features on different scales mislead the distance calculation
d) The pipeline itself, not the scaler, is responsible for the accuracy gain

13. In a GridSearchCV with a Pipeline, how do you specify a model hyperparameter in param_grid?

a) Use the hyperparameter name alone: n_neighbors
b) Use the model class name and hyperparameter: KNeighborsClassifier__n_neighbors
*c) Use the pipeline step name and hyperparameter joined by two underscores: knn__n_neighbors
d) Use the step index and hyperparameter: step1__n_neighbors

14. Why does cross_val_score use neg_mean_squared_error rather than mean_squared_error as the scoring argument?

a) Negative MSE penalizes under-prediction more heavily than over-prediction
b) cross_val_score only accepts negative scoring functions by design
*c) scikit-learn cross-validation assumes higher scores are better — negating MSE keeps that convention consistent
d) Negative MSE avoids overflow when errors are large

15. When is an interpretable model like linear regression preferred over a flexible one like KNN?

*a) When you need to explain to stakeholders how predictions were made — linear regression coefficients can be calculated and interpreted
b) When the dataset is very large and training speed is the priority
c) When the data has many missing values that require imputation
d) When the target variable is categorical rather than continuous

16. In the multi-model evaluation example from the chapter, which three classifiers are compared for binary genre classification?

a) Naive Bayes, SVM, and random forest
*b) KNN, logistic regression, and decision tree classifier
c) Ridge, Lasso, and ElasticNet regression
d) Gradient boosting, AdaBoost, and logistic regression

17. In the cross-validation boxplot comparing models, what does the orange line inside each box represent?

a) The mean cross-validation score across all folds
b) The best single-fold score
*c) The median cross-validation score across folds
d) The score on the held-out test set

18. After comparing three classifiers on the test set using accuracy, which model performed best in the chapter example?

a) KNN with k=6 neighbors
b) Decision tree classifier with default depth
*c) Logistic regression
d) All three tied at the same accuracy

19. A spam filter screens 500 emails. 100 are truly spam. The filter flags 80 as spam, of which 60 are truly spam. How many false positives does the filter produce?

a) 40
*b) 20
c) 60
d) 80

20. Using the same spam filter from question 19 (500 emails, 100 truly spam, 80 flagged, 60 correctly flagged), what is the recall?

*a) 0.60
b) 0.75
c) 0.80
d) 0.50

21. A hospital model detects cancer. Missing a cancer case is far more dangerous than ordering an unnecessary biopsy. Which metric should the model prioritize?

a) Accuracy
b) Precision
*c) Recall
d) F1 score

22. A sales team uses a model to flag high-value leads, but capacity allows only 10 follow-up calls per day. Acting on a false lead wastes a call slot. Which metric matters most?

a) Accuracy
*b) Precision
c) Recall
d) Specificity

23. A content moderation model removes posts. False positives suppress legitimate speech; false negatives leave harmful content online. Which metric best balances both errors?

a) Accuracy
b) Precision
c) Recall
*d) F1 score

24. A rare disease affects 2 percent of the population. A model that always predicts "healthy" achieves 98 percent accuracy. What does this illustrate?

a) The model is effective — 98 percent accuracy is an excellent result
b) Accuracy is reliable as long as the dataset has more than 1,000 examples
*c) Accuracy is misleading on imbalanced data — a trivial model matches the real model's score while catching zero cases
d) The model needs more training data to improve its recall

25. A credit model flags 120 applicants as high risk. Of those 120, 48 truly default. What is the model's precision?

a) 0.60
b) 0.80
*c) 0.40
d) 0.48
