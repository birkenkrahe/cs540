#+TITLE:CS 540 Quiz – Week 4 (June 10, 2026)
#+AUTHOR:Marcus Birkenkrahe
#+SUBTITLE:CS 540 - Principles of AI - Catholic Polytechnic U, Summer 2026

Note to students: This quiz is not a memory test. If you are unsure
about a question, open a Colab notebook and try it out — that is
exactly the kind of engagement with the language we are looking for.
The goal is learning, not guessing, or memorizing.

The quiz covers Week 4 content: the kNN loan approval code-along,
DataCamp review (Understanding ML, Classification), the Regression
chapter (DataCamp), and discussion of "Thinking Machines" (1961) and
"Computer Chronicles" (1984). It was auto-generated by Claude from the
session recording, DataCamp transcripts, and agenda notes, then
checked by the instructor. There might be mistakes — notify me if you
find one and earn bonus points!

* Movies: "Thinking Machines" (1961) and "Computer Chronicles" (1984)

1. How does "Thinking Machines" (1961) define machine intelligence?

- [X] Behaviorist: if it acts intelligent, it counts as intelligent
- [ ] Statistical: intelligence is learned from data
- [ ] Symbolic: intelligence is stored IF-THEN rules
- [ ] Connectionist: intelligence emerges from neural networks

2. What AI technology is the focus of "Computer Chronicles" (1984)?

- [ ] Neural networks
- [ ] Supervised learning
- [X] Expert systems
- [ ] Robotics

3. Why did expert systems collapse after their 1980s peak?

- [ ] They required too much labeled training data
- [ ] Neural networks replaced them immediately
- [X] They were brittle — IF-THEN rules failed in messy real-world cases
- [ ] Hardware could not run them fast enough

4. What key idea is absent from all AI depicted in "Thinking Machines"?

- [ ] Expert rules
- [ ] Probabilistic reasoning
- [X] Learning from data — every method shown relies on programmed rules
- [ ] Natural language processing

5. Who coined the term "artificial intelligence"?

- [ ] Claude Shannon
- [ ] Arthur Samuel
- [ ] Herbert Simon
- [X] John McCarthy

6. Arthur Samuel is notable in AI history for demonstrating that a machine could:

- [ ] Translate natural language
- [X] Beat humans at checkers through a form of self-learning
- [ ] Recognize faces in photographs
- [ ] Solve differential equations automatically

7. How did the professor characterize the current state of AI in the session?

- [ ] A slow recovery from the last AI winter
- [ ] Steady, balanced growth with no hype cycle
- [X] An "AI super summer" — interest and investment at historic highs
- [ ] A period of moderate optimism after a brief setback

8. What recurring pattern does AI history show?

- [X] Over-optimism → hidden limits emerge → funding collapses → slow recovery
- [ ] Exponential growth with no significant winters since 1984
- [ ] Steady linear improvement punctuated by hardware breakthroughs
- [ ] Random fluctuations driven mainly by government policy

* DataCamp Review (Understanding ML / Classification)

9. Why is 97% accuracy misleading for a fraud detection model?

- [ ] The model misclassifies too many legitimate transactions
- [X] Fraud is rare; predicting "not fraud" always achieves high accuracy while catching zero fraud
- [ ] 97% falls below the financial industry standard
- [ ] The model was trained on too little data

10. How does a regression ensemble combine predictions from multiple models?

- [ ] It picks the model with the highest training accuracy
- [X] It averages predictions, reducing variance by canceling independent errors
- [ ] It uses a second neural network to weigh each model's output
- [ ] It selects the median prediction

11. What do early hidden layers of a face recognition network typically learn?

- [ ] Complete faces and identities
- [ ] Names associated with each face
- [X] Low-level features such as edges and color transitions
- [ ] Demographic attributes like age and gender

12. If an unsupervised algorithm runs on data with no real patterns, what happens?

- [ ] It returns an error or empty result
- [X] It still produces clusters — they may just be geometric artifacts with no meaning
- [ ] It produces exactly one cluster containing all points
- [ ] It switches to supervised mode automatically

13. Which of these is NOT a required preprocessing step for supervised ML?

- [ ] Handling missing values
- [ ] Encoding features as numeric values
- [X] Removing all duplicate rows before any other step
- [ ] Storing data in arrays or data frames

14. In scikit-learn, why is model.fit() called with X, y?

- [X] X is the 2D feature matrix (rows = instances, columns = features); y is the 1D label vector
- [ ] X holds labels and y holds features by convention
- [ ] Python requires alphabetical argument order
- [ ] The order is arbitrary — both arguments are equivalent

15. In kNN, "nearest" refers to:

- [ ] The most similar class label
- [X] Smallest distance in feature space, e.g. Euclidean distance or cosine similarity
- [ ] Highest Pearson correlation between vectors
- [ ] Closest position in the time-series order of the dataset

16. What does the stratify=y argument in train_test_split ensure?

- [ ] Training always uses more data than testing
- [X] The class label proportions in the original data are preserved in both splits
- [ ] All minority-class examples go into the test set
- [ ] The split ratio is determined automatically

17. What is the risk of very low k (e.g., k=1) in kNN?

- [X] Overfitting — the boundary is jagged and sensitive to individual training points
- [ ] Underfitting — the boundary is too smooth
- [ ] The model becomes slow to train
- [ ] Accuracy always improves as k decreases

18. What is the purpose of random_state=42 in train_test_split?

- [ ] It limits the split to 42 percent training data
- [ ] It removes 42 outliers before splitting
- [X] It seeds the random number generator so the split is identical every run
- [ ] It ensures 42 observations always appear in the test set

* kNN Code-Along: Loan Approval in 2D

19. Why must x_new be np.array([[4, 5]]) rather than np.array([4, 5]])?

- [X] scikit-learn requires 2D input; [[4,5]] has shape (1,2), [4,5] has shape (2,) which is 1D
- [ ] Double brackets encode income and credit score separately
- [ ] np.array([4, 5]) produces floats, causing a type error
- [ ] NumPy arrays must always have even dimensions

20. Why does kNN prefer odd values of k?

- [ ] scikit-learn only accepts odd k
- [ ] Odd k values train faster
- [X] Odd k avoids ties in the majority vote — there is always a winning class
- [ ] Even k values produce unstable decision boundaries

21. What does np.meshgrid produce in the decision region code?

- [X] A grid of coordinate pairs covering the 2D feature space for batch prediction
- [ ] A matrix of distances between training points
- [ ] The confusion matrix of the model
- [ ] A plot of the loss function over training epochs

22. What does plt.contourf show in the kNN visualization?

- [ ] The training points colored by true label
- [X] Filled color regions showing which class the model predicts at each location in 2D space
- [ ] The distribution of feature values
- [ ] Model prediction error across the feature space

23. What does X[y==0, 0] return in the loan approval dataset?

- [ ] All rows where credit score is 0
- [X] The income values for all customers whose loan was denied (label 0)
- [ ] The first row of the feature matrix
- [ ] All customers with income equal to 0

24. Why is kNN called a "lazy learner"?

- [ ] It converges slowly during training
- [X] knn.fit() only stores the data; all work happens at prediction time when distances are computed
- [ ] It requires extensive hyperparameter tuning
- [ ] It is designed only for small datasets

25. In the session demo, both k=1 and k=3 predicted approval for x_new=[4,5]. Why?

- [ ] They always agree for these two values of k
- [X] The new point fell clearly in the approved region — both the nearest and the majority of 3 neighbors were labeled 1
- [ ] The dataset is too small for k to matter
- [ ] Only k=3 predicted approval; k=1 was undecided

26. What does shape (6, 2) mean for the feature matrix X?

- [X] 6 training customers, each described by 2 features (income, credit score)
- [ ] 6 features and 2 training examples
- [ ] 6 rows and 2 output classes
- [ ] Income ranges from 2 to 6 and credit scores from 2 to 6

27. What is the "decision boundary" in the kNN visualization?

- [ ] The line separating training data from test data
- [ ] The threshold accuracy above which a model is accepted
- [X] The boundary in feature space where the model switches its prediction from one class to another
- [ ] The optimal k value for the dataset

28. In the loan example, why does y have shape (6,) while x_new has shape (1, 2)?

- [ ] y is a list, x_new is an array
- [X] y is a 1D label vector (one label per sample); x_new is a 2D array because scikit-learn requires it
- [ ] y has 6 features; x_new has 1 feature and 2 samples
- [ ] Shapes are interchangeable — either format works

* DataCamp Regression Chapter

29. What distinguishes a regression problem from a classification problem?

- [ ] Regression uses labeled data; classification does not
- [X] The target variable is continuous in regression; discrete in classification
- [ ] Regression models cannot be evaluated on a test set
- [ ] Classification uses more features than regression

30. In y = ax + b (simple linear regression), what are a and b called?

- [ ] Hyperparameters
- [ ] Residuals and loss values
- [X] Model coefficients: a is the slope, b is the intercept
- [ ] Features and labels

31. What is a residual in linear regression?

- [X] The vertical distance between an observed data point and the regression line
- [ ] The model's R-squared score after training
- [ ] The difference between training and test accuracy
- [ ] The penalty term added by regularization

32. Why does OLS square the residuals rather than summing them directly?

- [X] To prevent positive and negative residuals from canceling each other out
- [ ] To make the loss function differentiable at all points
- [ ] To penalize large coefficients specifically
- [ ] To speed up convergence

33. What does R-squared measure?

- [X] The proportion of target variance explained by the features (0 = none, 1 = complete)
- [ ] The average distance between predictions and true values
- [ ] The square root of the mean squared error
- [ ] The ratio of training accuracy to test accuracy

34. Why is RMSE preferred over MSE for reporting model error?

- [ ] RMSE is always smaller and easier to interpret
- [X] RMSE is in the same units as the target variable; MSE is in squared units
- [ ] RMSE and MSE are identical; RMSE is just faster to compute
- [ ] RMSE penalizes only positive errors

35. What problem does cross-validation solve compared to a single train/test split?

- [ ] It eliminates the need to tune hyperparameters
- [X] It reduces dependence on one random split — any single split may produce an unrepresentative test set
- [ ] It allows training on the test set safely
- [ ] It selects the best model architecture automatically

36. In 5-fold cross-validation, how many times is the model trained?

- [ ] Once, on the full dataset
- [ ] Twice — once on each half
- [X] Five times — each fold serves as the test set exactly once
- [ ] Ten times — each fold is used for both training and testing

37. What is the trade-off of using more folds in k-fold cross-validation?

- [X] More reliable performance estimates, but higher computational cost
- [ ] Better final model accuracy, but more overfitting
- [ ] Faster training but less reliable estimates
- [ ] More labeled data required

38. What is regularization in regression, and why is it applied?

- [ ] A preprocessing step that scales features before training
- [X] A modification of the loss function that penalizes large coefficients to prevent overfitting
- [ ] An ensemble method combining several regression models
- [ ] A technique that increases model complexity

39. In Ridge regression, what is the effect of setting alpha very high?

- [ ] The model overfits because coefficients are not penalized
- [X] The model underfits — large coefficients are heavily penalized, reducing flexibility
- [ ] Alpha has no effect once the model is fitted
- [ ] Performance improves continuously as alpha increases

40. How does Lasso regression differ from Ridge in what it does to coefficients?

- [ ] Lasso squares the coefficients in the penalty; Ridge uses absolute values
- [X] Lasso uses absolute values and can shrink some coefficients to exactly zero, enabling feature selection
- [ ] Lasso and Ridge are identical except for their default alpha values
- [ ] Lasso penalizes only positive coefficients
