#+TITLE:CS 540 Quiz – Week 4 (June 10, 2026)
#+AUTHOR:Marcus Birkenkrahe
#+SUBTITLE:CS 540 - Principles of AI - Catholic Polytechnic U, Summer 2026

Note to students: This quiz is not a memory test. Open a Colab
notebook if you are unsure — that is exactly the engagement we are
looking for.

The quiz covers Week 4 content: the kNN loan approval code-along,
DataCamp review (Understanding ML and Classification chapters), the
DataCamp Regression chapter, and discussion of "Thinking Machines"
(1961) and "Computer Chronicles" (1984). Auto-generated by Claude from
the session recording, transcripts, and agenda notes; checked by the
instructor. Mistakes are possible — flag them for bonus points!

Each question is worth 1 point. Total: 30 points.

1. How does "Thinking Machines" (1961) define machine intelligence?

- [X] Behaviorist: if it acts intelligent, it counts as intelligent
- [ ] Statistical: intelligence is learned from data
- [ ] Symbolic: intelligence is stored IF-THEN rules
- [ ] Connectionist: it emerges from neural networks

2. What AI technology is the focus of "Computer Chronicles" (1984)?

- [ ] Neural networks
- [ ] Supervised learning
- [X] Expert systems
- [ ] Robotics

3. Why did expert systems collapse after their 1980s peak?

- [ ] Neural networks replaced them immediately
- [X] They were brittle — IF-THEN rules failed in messy real-world cases
- [ ] Hardware could not run them fast enough
- [ ] They required too much labeled training data

4. What key idea is absent from the AI shown in "Thinking Machines"?

- [ ] Expert rules
- [ ] Probabilistic reasoning
- [X] Learning from data — all methods rely on programmed rules
- [ ] Natural language processing

5. What recurring pattern does AI history show?

- [X] Over-optimism, then hidden limits emerge, funding collapses, slow recovery
- [ ] Exponential growth with no significant winters since 1984
- [ ] Steady linear improvement driven by hardware breakthroughs
- [ ] Random fluctuations with no predictable cycle

6. Why is 97% accuracy misleading for a fraud detection model?

- [ ] The model misclassifies too many legitimate transactions
- [X] Fraud is rare; predicting "not fraud" always scores high while catching zero fraud
- [ ] 97% falls below the financial industry standard
- [ ] The model was trained on too little data

7. How does a regression ensemble combine predictions?

- [ ] Picks the model with the highest training accuracy
- [X] Averages predictions, reducing variance by canceling independent errors
- [ ] Uses a second neural network to weigh each model
- [ ] Selects the median prediction

8. What do early hidden layers of a face recognition network learn?

- [ ] Complete faces and identities
- [X] Low-level features such as edges and color transitions
- [ ] Names associated with each face
- [ ] Demographic attributes like age

9. If an unsupervised algorithm runs on data with no real patterns, what happens?

- [ ] It returns an error or empty result
- [X] It still produces clusters — they may be geometric artifacts with no real meaning
- [ ] It produces exactly one cluster
- [ ] It switches to supervised mode automatically

10. Which of these is NOT a required preprocessing step for supervised ML?

- [ ] Handling missing values
- [ ] Encoding features as numeric values
- [X] Removing all duplicate rows before any other step
- [ ] Storing data in arrays or data frames

11. In model.fit(X, y), what is X?

- [X] A 2D matrix: rows are training instances, columns are features
- [ ] The 1D vector of target labels
- [ ] A list of model hyperparameters
- [ ] The test set reserved for evaluation

12. In kNN, "nearest" means:

- [ ] Most similar class label
- [X] Smallest distance in feature space (e.g., Euclidean or cosine)
- [ ] Highest Pearson correlation between vectors
- [ ] Closest position in time-series order

13. What does stratify=y in train_test_split ensure?

- [ ] Training always uses more data than testing
- [X] Class label proportions in the original data are preserved in both splits
- [ ] All minority-class examples go into the test set
- [ ] The split ratio is determined automatically

14. What is the risk of very low k (e.g., k=1) in kNN?

- [X] Overfitting — the boundary is jagged and sensitive to individual points
- [ ] Underfitting — the boundary is too smooth
- [ ] The model becomes slow to train
- [ ] Accuracy always improves as k decreases

15. What is the purpose of random_state=42 in train_test_split?

- [ ] It limits the split to 42 percent training data
- [ ] It removes 42 outliers before splitting
- [X] It seeds the random generator so the same split is produced every run
- [ ] It ensures 42 observations always appear in the test set

16. Why must x_new be ~np.array([[4, 5]])~ rather than np.array([4, 5])?

- [X] scikit-learn requires 2D input; ~[[4,5]]~ has shape (1,2), [4,5] has shape (2,)
- [ ] Double brackets encode income and credit score separately
- [ ] np.array([4, 5]) produces floats, causing a type error
- [ ] NumPy arrays must always have even dimensions

17. Why does kNN prefer odd values of k?

- [ ] scikit-learn only accepts odd k
- [ ] Odd k values train faster
- [X] Odd k avoids ties — there is always a majority class in the vote
- [ ] Even k values produce unstable decision boundaries

18. What does plt.contourf show in the kNN visualization?

- [ ] Training points colored by true label
- [X] Filled color regions showing which class the model predicts across the 2D space
- [ ] Distribution of feature values
- [ ] Model prediction error across the feature space

19. Why is kNN called a "lazy learner"?

- [ ] It converges slowly during training
- [X] knn.fit() only stores the data; all computation happens at prediction time
- [ ] It requires extensive hyperparameter tuning
- [ ] It is designed only for small datasets

20. What is the "decision boundary" in the kNN plot?

- [ ] The line separating training data from test data
- [ ] The threshold accuracy above which a model is accepted
- [X] The boundary where the model switches its prediction from one class to another
- [ ] The optimal k value for the dataset

21. What does shape (6, 2) mean for the feature matrix X?

- [X] 6 training customers, each with 2 features (income, credit score)
- [ ] 6 features and 2 training examples
- [ ] 6 rows and 2 output classes
- [ ] Income ranges from 2 to 6 and credit scores from 2 to 6

22. What distinguishes a regression problem from a classification problem?

- [ ] Regression uses labeled data; classification does not
- [X] The target variable is continuous in regression; discrete in classification
- [ ] Regression models cannot be evaluated on a test set
- [ ] Classification uses more features than regression

23. What is a residual in linear regression?

- [X] The vertical distance between an observed data point and the regression line
- [ ] The model's R-squared score after training
- [ ] The difference between training and test accuracy
- [ ] The penalty term added by regularization

24. Why does OLS square the residuals rather than summing them directly?

- [X] To prevent positive and negative residuals from canceling each other out
- [ ] To make the loss function differentiable at all points
- [ ] To penalize large coefficients specifically
- [ ] To speed up convergence

25. What does R-squared measure?

- [X] The proportion of target variance explained by the features (0 to 1)
- [ ] The average distance between predictions and true values
- [ ] The square root of the mean squared error
- [ ] The ratio of training accuracy to test accuracy

26. Why is RMSE preferred over MSE for reporting model error?

- [ ] RMSE is always smaller and easier to interpret
- [X] RMSE is in the same units as the target; MSE is in squared units
- [ ] They are identical; RMSE is just faster to compute
- [ ] RMSE penalizes only positive errors

27. What problem does cross-validation solve?

- [ ] It eliminates the need to tune hyperparameters
- [X] It reduces dependence on one random split that may give an unrepresentative test set
- [ ] It allows training on the test set safely
- [ ] It selects the best model architecture automatically

28. In 5-fold cross-validation, how many times is the model trained?

- [ ] Once, on the full dataset
- [ ] Twice — once on each half
- [X] Five times — each fold serves as the test set exactly once
- [ ] Ten times — each fold is used twice

29. What is regularization in regression?

- [ ] A preprocessing step that scales feature values
- [X] A loss function modification that penalizes large coefficients to prevent overfitting
- [ ] An ensemble method combining several regression models
- [ ] A technique that adds more features to the model

30. In Ridge regression, what is the effect of setting alpha very high?

- [ ] The model overfits because coefficients are not penalized
- [X] The model underfits — large coefficients are heavily penalized, reducing flexibility
- [ ] Alpha has no effect once the model is fitted
- [ ] Performance improves continuously as alpha increases
