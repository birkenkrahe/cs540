#+TITLE:CS 540 Quiz – Week 5 (June 17, 2026)
#+AUTHOR:Marcus Birkenkrahe
#+SUBTITLE:CS 540 - Principles of AI - Catholic Polytechnic U, Summer 2026

Note to students: This quiz is not a memory test. Open a Colab
notebook if you are unsure — that is exactly the engagement we are
looking for.

The quiz covers Week 5 content: the DataCamp "Fine-Tuning Your Model"
chapter (classification metrics, logistic regression, ROC/AUC,
hyperparameter tuning), and the June 17 session (end-to-end ML
pipeline, framing the problem, RMSE, Ridge vs. Lasso geometry).
Auto-generated by Claude from session recording, transcripts, and
slides; checked by the instructor. Mistakes are possible — flag them
for bonus points!

Each question is worth 1 point. Total: 30 points.

Late submission penalty: 50%

1. Why is 99% accuracy misleading for a fraud detector where only 1%
   of transactions are fraudulent?

- [X] A model predicting "not fraud" always achieves 99% while catching zero fraud
- [ ] 99% falls below the financial industry's required threshold
- [ ] The model was trained on too little data
- [ ] Accuracy is only misleading when the dataset has missing values

2. What is class imbalance?

- [ ] More features than observations in the training set
- [X] One class appears far more frequently than another
- [ ] The model assigns unequal weights to features
- [ ] Test accuracy is higher than training accuracy

3. In a binary confusion matrix, true negatives are:

- [ ] Positive instances correctly classified as positive
- [ ] Negative instances incorrectly classified as positive
- [X] Negative instances correctly classified as negative
- [ ] Positive instances incorrectly classified as negative

4. What does precision measure in a classification model?

- [ ] The fraction of all positive instances the model correctly identifies
- [X] Of all instances predicted positive, the fraction that truly are positive
- [ ] The harmonic mean of sensitivity and specificity
- [ ] Total correct predictions divided by all predictions

5. What does recall (sensitivity) measure in a classification model?

- [X] Of all truly positive instances, the fraction the model correctly flags
- [ ] Of all predicted positives, the fraction that are truly positive
- [ ] The square root of the product of precision and specificity
- [ ] Ratio of true positives to true negatives

6. What does the F1 score compute?

- [ ] Accuracy of the positive class only
- [ ] Precision minus recall, normalized to [0, 1]
- [X] The harmonic mean of precision and recall
- [ ] Area under the precision-recall curve

7. Despite its name, logistic regression is used for:

- [ ] Predicting continuous numeric targets
- [X] Binary classification — it outputs a probability and applies a threshold
- [ ] Clustering observations into groups
- [ ] Dimensionality reduction before classification

8. With the default threshold of 0.5, a predicted probability of 0.3
   yields label:

- [ ] 1, because p is closer to 0.5 than to 0
- [X] 0, because p is below the threshold
- [ ] Undefined — the model abstains when p is not extreme
- [ ] 1, because logistic regression always predicts the majority class

9. What kind of decision boundary does logistic regression produce?

- [ ] A curved boundary wrapping around each class cluster
- [ ] A boundary based on nearest-neighbor distances
- [X] A linear decision boundary
- [ ] A tree-structured boundary

10. What does the ROC curve plot?

- [ ] Precision vs. recall at different thresholds
- [ ] Training loss vs. validation loss
- [X] True positive rate vs. false positive rate at different thresholds
- [ ] Model accuracy vs. number of neighbors k

11. What does AUC of 0.5 indicate?

- [ ] The model perfectly separates the two classes
- [ ] The model has very high recall but low precision
- [X] The model performs no better than random guessing
- [ ] The model predicts only the majority class

12. What is a hyperparameter, distinct from a model parameter?

- [ ] A parameter the model learns from training data
- [X] A value set before training that controls the learning process, such as alpha or n_neighbors
- [ ] The intercept term in a linear model
- [ ] Any parameter appearing in the loss function

13. Why must hyperparameter tuning use cross-validation, not the test set?

- [ ] Cross-validation is faster than evaluating on the test set
- [ ] The test set is too small for reliable scores
- [X] To avoid overfitting the hyperparameters to the test set
- [ ] GridSearchCV does not accept a test set as input

14. With 3-fold CV, 2 hyperparameters, and 5 values each, how many
    fits does grid search require?

- [ ] 10
- [ ] 15
- [X] 30 (2 times 5 times 3)
- [ ] 50

15. How does RandomizedSearchCV differ from GridSearchCV?

- [ ] It uses a different cross-validation strategy
- [ ] It evaluates all combinations but in random order
- [X] It samples a random subset of combinations rather than exhaustively searching
- [ ] It applies only to classification, not regression

16. What property of the California housing dataset justified batch learning?

- [ ] Data stream in continuously from sensors
- [X] The data are small, static, and not changing fast — processable offline
- [ ] The dataset has no missing values
- [ ] Batch learning is always preferred for regression

17. What does "univariate multiple regression" mean?

- [ ] One feature predicts one target
- [X] Multiple features predict a single continuous target variable
- [ ] Multiple features predict multiple targets
- [ ] One feature predicts a categorical target

18. Why does RMSE preserve the unit of the target variable?

- [ ] The mean operation normalizes units to a percentage
- [X] Taking the square root of squared dollars gives dollars — error is interpretable in the same scale as the target
- [ ] RMSE is unit-free and comparable across datasets
- [ ] The formula cancels units algebraically

19. When should you prefer MAE over RMSE?

- [ ] When you want to penalize large errors more heavily
- [ ] When the dataset is balanced with no outliers
- [X] When data have outliers — MAE's absolute value does not amplify large errors the way squaring does
- [ ] MAE and RMSE penalize errors identically

20. Why does Lasso set some coefficients exactly to zero while Ridge
    only shrinks them?

- [ ] Lasso uses a stronger penalty than Ridge by default
- [ ] Ridge adds a bias term preventing zero coefficients
- [X] The L1 constraint (diamond) has corners on the axes where the RSS ellipse first touches, making exact zeros likely
- [ ] Lasso applies cross-validation internally while Ridge does not

21. How does the L1/L2 geometry of Lasso/Ridge apply to MAE vs. RMSE?

- [ ] It does not apply — geometry is specific to regularization
- [X] MAE is L1 (diamond), RMSE is L2 (circle) — same geometry explains why L1 is more robust to outliers
- [ ] RMSE corresponds to a diamond because squaring creates corners
- [ ] The geometry only applies when features and losses use the same norm

22. What does the following Python code compute?
    rmse = np.sqrt(np.mean((predicted - actual) ** 2))

- [ ] Mean absolute error between predicted and actual values
- [X] Root mean squared error between predicted and actual values
- [ ] R-squared score of the model
- [ ] Standard deviation of the residuals

23. In the RMSE formula RMSE(X, h), what does h(x^(i)) represent?

- [ ] The actual target label for instance i
- [ ] The number of features in instance i
- [X] The model's predicted value for instance i
- [ ] The squared residual for instance i

24. Which scikit-learn function (introduced in v1.4) computes RMSE
    directly, without needing np.sqrt?

- [ ] mean_squared_error from sklearn.metrics
- [X] root_mean_squared_error from sklearn.metrics
- [ ] rmse_score from sklearn.metrics
- [ ] regression_report from sklearn.metrics

25. What does logreg.predict_proba(X_test)[:, 1] extract?

- [ ] Predicted class labels for all test instances
- [X] Probability of belonging to the positive class for each test instance
- [ ] Log-odds of the negative class for each instance
- [ ] The classification threshold applied to each instance

26. After fitting a GridSearchCV object, which attribute gives the
    best hyperparameter combination found?

- [ ] best_score_
- [X] best_params_
- [ ] cv_results_
- [ ] estimator_

27. In the housing problem, what motivated replacing the expert-based
    system with ML?

- [ ] Experts had access to too much data to process manually
- [X] The approach was costly, error-prone, and based on opaque rules maintained by humans
- [ ] Experts refused to share their pricing models
- [ ] The rules were too accurate, leaving no room for improvement

28. What four elements should a CS 540 midterm project outline include?

- [ ] Dataset, model choice, accuracy metric, and conclusion
- [X] Rationale, what you will look for, references, and what could go wrong
- [ ] Introduction, methodology, results, and discussion
- [ ] Topic, tools, timeline, and team members

29. What does higher exponent in a loss function (e.g., cubic vs.
    squared) imply about sensitivity to outliers?

- [ ] Higher exponents make the loss function more robust to outliers
- [ ] The exponent has no effect on sensitivity to large values
- [X] Higher exponents amplify the contribution of large errors — outliers dominate the loss
- [ ] Higher exponents are only relevant for regularization, not loss functions

30. What does the output of one ML model being fed as input to
    another ML model illustrate?

- [ ] Transfer learning between two neural networks
- [ ] Ensemble learning by combining model outputs
- [X] A data science pipeline — end-to-end from upstream data to downstream insights
- [ ] Online learning where the model updates continuously
